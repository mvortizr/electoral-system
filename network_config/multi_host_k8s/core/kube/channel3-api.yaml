
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: channel3-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: channel3-api
  template:
    metadata:
      labels:
        app: channel3-api
    spec:
      containers:
        - name: main
          image: docker.io/mvortizr/channel3_api:v1.7
          command: ["npm", "run", "start:prod"]
          imagePullPolicy: IfNotPresent 
          envFrom:
            - configMapRef:
                name: channel3-configmap
          ports:
            - containerPort: 3000

---

apiVersion: v1
kind: Service
metadata:
  name: channel3-api
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: channel3-api
---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/proxy-connect-timeout: 60s
  labels:
    app: channel3-api
  name: channel3-api
spec:
  ingressClassName: nginx
  rules:
    - host: channel3-api.${DOMAIN}
      http:
        paths:
          - backend:
              service:
                name: channel3-api
                port:
                  name: http
            path: /
            pathType: ImplementationSpecific
